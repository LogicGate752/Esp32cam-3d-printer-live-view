# Esp32cam-3d-printer-live-view
I built a local 3D printer monitor using an ESP32-CAM and Arduino IDE. It runs a custom webpage on a local server to stream live video, toggle a flashlight, and snap photos. I assembled a protective case to keep the wiring safe from the printer frame and optimized the code to run smoothly on a tiny 4MB chip without relying on any cloud apps!
